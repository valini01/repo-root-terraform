name: "🏭 Deploy Production (Live LV)"

on:
  pull_request:
    types: [closed]
    branches:
      - main

jobs:
  validate:
    if: github.event.pull_request.merged == true
    uses: ./.github/workflows/terraform-validate.yml
    with:
      environment: lv
    secrets: inherit

  plan:
    needs: validate
    uses: ./.github/workflows/terraform-plan.yml
    with:
      environment: lv
    secrets: inherit

  apply:
    needs: plan
    uses: ./.github/workflows/terraform-apply.yml
    with:
      environment: lv
    secrets: inherit

  destroy:
    needs: apply
    uses: ./.github/workflows/terraform-destroy.yml
    with:
      environment: lv
    secrets: inherit  

  cleanup-feature-branch:
    name: "🧹 Delete Deployed Feature Branch"
    needs: apply
    if: success()
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Generate GitHub App Token
        id: app-token
        uses: actions/create-github-app-token@v1
        with:
          app_id: ${{ secrets.APP_ID }}
          private_key: ${{ secrets.PRIVATE_KEY }}

      - name: Set GH_TOKEN
        run: |
          echo "GH_TOKEN=${{ steps.app-token.outputs.token }}" >> $GITHUB_ENV
          echo "✅ Authenticated with GitHub App successfully"

      - name: Identify merged feature branch
        id: find_branch
        env:
          GH_TOKEN: ${{ steps.auth.outputs.token }}
        run: |
          # Get the branch from the most recent merged PR to staging
          FEATURE_BRANCH=$(gh pr list --repo ${{ github.repository }} --state merged --base staging --limit 1 --json headRefName --jq '.[0].headRefName' || echo "")
          echo "feature_branch=$FEATURE_BRANCH" >> $GITHUB_OUTPUT
          echo "🔍 Found merged feature branch: $FEATURE_BRANCH"

      - name: Delete remote feature branch
        if: steps.find_branch.outputs.feature_branch != ''
        env:
          GH_TOKEN: ${{ steps.auth.outputs.token }}
        run: |
          BRANCH=${{ steps.find_branch.outputs.feature_branch }}
          echo "🧹 Deleting remote branch: $BRANCH"
          git push https://x-access-token:${GH_TOKEN}@github.com/${{ github.repository }} --delete $BRANCH || echo "⚠️ Could not delete $BRANCH"
          echo "✅ Cleanup complete — staging and main remain."
