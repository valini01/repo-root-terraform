name: "🎛️ Combined Multi-Environment Pipeline"

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Select target environment'
        required: true
        default: 'lab'
        type: choice
        options:
        - lab
        - nlv
        - lv
      action:
        description: 'Select action to perform'
        required: true
        default: 'plan'
        type: choice
        options:
        - validate
        - plan
        - apply
        - destroy
      reason:
        description: 'Reason for manual deployment'
        required: true
        type: string

env:
  SELECTED_ENVIRONMENT: ${{ github.event.inputs.environment }}
  SELECTED_ACTION: ${{ github.event.inputs.action }}

jobs:
  # 🚦 STEP 1: Always validate first (unless destroying)
  terraform-validate:
    name: "✅ Terraform Validate (${{ github.event.inputs.environment }})"
    if: inputs.action == 'validate' || inputs.action == 'plan' || inputs.action == 'apply'
    uses: ./.github/templates/terraform-validate.yml
    with:
      environment: ${{ env.SELECTED_ENVIRONMENT }}
    secrets:
      AZURE_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
      AZURE_CLIENT_SECRET: ${{ secrets.AZURE_CLIENT_SECRET }}
      AZURE_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
      AZURE_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}

  # 📋 STEP 2: Plan the deployment
  terraform-plan:
    name: "📋 Terraform Plan (${{ github.event.inputs.environment }})"
    if: inputs.action == 'plan' || inputs.action == 'apply'
    needs: terraform-validate
    uses: ./.github/templates/terraform-plan.yml
    with:
      environment: ${{ env.SELECTED_ENVIRONMENT }}
    secrets:
      AZURE_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
      AZURE_CLIENT_SECRET: ${{ secrets.AZURE_CLIENT_SECRET }}
      AZURE_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
      AZURE_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}

  # 🚀 STEP 3: Apply the deployment (with approval gates)
  terraform-apply:
    name: "🚀 Terraform Apply (${{ github.event.inputs.environment }}) - Manual"
    if: inputs.action == 'apply'
    needs: terraform-plan
    uses: ./.github/templates/terraform-apply.yml
    with:
      environment: ${{ env.SELECTED_ENVIRONMENT }}
    secrets:
      AZURE_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
      AZURE_CLIENT_SECRET: ${{ secrets.AZURE_CLIENT_SECRET }}
      AZURE_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
      AZURE_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}

  # 💥 STEP 4: Destroy infrastructure (with approval gates)
  terraform-destroy:
    name: "💥 Terraform Destroy (${{ github.event.inputs.environment }}) - Manual"
    if: inputs.action == 'destroy'
    uses: ./.github/templates/terraform-destroy.yml
    with:
      environment: ${{ env.SELECTED_ENVIRONMENT }}
    secrets:
      AZURE_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
      AZURE_CLIENT_SECRET: ${{ secrets.AZURE_CLIENT_SECRET }}
      AZURE_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
      AZURE_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}

  # 📊 STEP 5: Log manual deployment
  log-manual-deployment:
    name: "📊 Log Manual Deployment"
    if: always() && (needs.terraform-apply.result == 'success' || needs.terraform-destroy.result == 'success')
    needs: [terraform-apply, terraform-destroy]
    runs-on: ubuntu-latest
    
    steps:
    - name: Create manual deployment log
      uses: actions/github-script@v7
      with:
        script: |
          const status = '${{ needs.terraform-apply.result }}' === 'success' ? 'Applied' : 
                        '${{ needs.terraform-destroy.result }}' === 'success' ? 'Destroyed' : 'Unknown';
          
          const logEntry = `## 📊 Manual Deployment Log
          
          **Action:** ${status}
          **Environment:** ${{ env.SELECTED_ENVIRONMENT }}
          **Performed by:** @${{ github.actor }}
          **Reason:** ${{ github.event.inputs.reason }}
          **Timestamp:** ${new Date().toISOString()}
          **Workflow Run:** ${{ github.run_id }}
          
          ### Details
          - **Repository:** ${{ github.repository }}
          - **Branch:** ${{ github.ref_name }}
          - **Commit:** ${{ github.sha }}
          - **Action:** ${{ env.SELECTED_ACTION }}
          - **Environment:** ${{ env.SELECTED_ENVIRONMENT }}
          
          **Manual deployment completed via Combined Pipeline.**
          `;
          
          github.rest.issues.create({
            owner: context.repo.owner,
            repo: context.repo.repo,
            title: `📊 Manual ${status} - ${{ env.SELECTED_ENVIRONMENT }} - ${new Date().toISOString().split('T')[0]}`,
            body: logEntry,
            labels: ['manual-deployment', '${{ env.SELECTED_ENVIRONMENT }}', '${{ env.SELECTED_ACTION }}']
          });